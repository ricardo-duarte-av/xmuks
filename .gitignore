# Gradle / Kotlin
.gradle/
.kotlin/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
.vscode/

# Local machine config
local.properties
*.log
hs_err_pid*
replay_pid*
captures/

# Release signing — never commit (CI decodes it from secrets)
keystore.properties
*.jks
*.keystore

# Local agent notes (may contain credentials) — never commit
CLAUDE.md
CLAUDE.local.md
.claude/
