<resources>
    <!-- Shown by the SplashScreen API while the process starts; hands off to Theme.Xmuks. -->
    <style name="Theme.Xmuks.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Xmuks</item>
    </style>

    <!-- Compose draws everything; the window theme only needs to be edge-to-edge friendly. -->
    <style name="Theme.Xmuks" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_background</item>
    </style>
</resources>
