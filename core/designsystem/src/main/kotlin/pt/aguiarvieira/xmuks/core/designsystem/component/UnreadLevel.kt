package pt.aguiarvieira.xmuks.core.designsystem.component

/** Loudest first: a mention (error colour), then a notification count, then a quiet dot. */
enum class UnreadLevel { None, Dot, Count, Mention }
