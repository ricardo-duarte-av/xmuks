// Types the Compose compiler can't analyse (other modules, no compose plugin) but that are
// deeply immutable in practice. Marking them stable lets unchanged list rows skip recomposition
// instead of redrawing on every sync.
pt.aguiarvieira.xmuks.core.data.rooms.*
pt.aguiarvieira.xmuks.core.data.connection.StreamStats
pt.aguiarvieira.xmuks.core.network.ConnectionState
pt.aguiarvieira.xmuks.core.network.ConnectionState.*
// Our lists are always fresh read-only copies from the database layer; never mutated in place.
kotlin.collections.List
